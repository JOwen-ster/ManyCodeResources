// Get element by its id
idEle = document.getElementById('myid')

// Get element by its class
classEle = document.getElementsByClassName('myclass')

// Get element by its tag
tagEle = document.getElementsByTagName('h1')

// Get element by its query selector ('.class', '#id', 'tag')
queryEle = document.querySelector('h3')

// Get element by its query selector all ('.class', '#id', 'tag')
queryEles = document.querySelectorAll('div')

const headingOne = document.getElementById('myid')
headingOne.style.color = 'red'